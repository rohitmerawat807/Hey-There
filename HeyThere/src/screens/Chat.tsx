import { View, Text } from 'react-native';

function Chat() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Chat</Text>
        </View>
    );
}

export default Chat;